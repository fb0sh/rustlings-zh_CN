fn main() {
    let cat = ("Furry McFurson", 3.5);

    // TODO: Destructure the `cat` tuple in one statement so that the println works.
    // TODO: 使用一条语句解构 `cat` 元组，使下面的 println! 可以正常工作
    // let /* your pattern here */ = cat;

    println!("{name} is {age} years old");
}
