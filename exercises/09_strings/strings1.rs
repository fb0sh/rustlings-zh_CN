// TODO: Fix the compiler error without changing the function signature.
// TODO: 在不修改函数签名的情况下修复编译错误

fn current_favorite_color() -> String {
    "blue"
}

fn main() {
    let answer = current_favorite_color();
    println!("My current favorite color is {answer}");
}
